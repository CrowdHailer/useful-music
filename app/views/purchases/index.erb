<main class="content">
    <aside>
      <ul>
        <li>
          <a href="/purchases/new">New Purchase</a>
        </li>
        <li>
          <a href="/">Back</a>
        </li>
      </ul>
    </aside>
    <article>
        <h2>Customers</h2>
        <p>
          Add Pagination
        </p>
        <table class="pure-table">
            <thead>
                <tr>
                    <th>Piece</th>
                    <th>Part</th>
                    <th>Quantity</th>
                    <th>url</th>
                    <!-- <th>email</th> -->
                    <!-- <th colspan=3>Action</th> -->
                </tr>
            </thead>

            <tbody>
                <% @purchases.each do |purchase| %>
                    <tr>
                        <th>
                            <%= purchase.item.piece.product_name %>
                        </th>
                        <th>
                            <%= purchase.item.name %>
                        </th>
                        <th>
                            <%= purchase.quantity %>
                        </th>
                        <th>
                            <a href="<%= purchase.item.asset.url %>">asset</a>
                        </th>
                        <!-- <th>
                            <a href="/purchases/<%= purchase.id %>">show</a>
                        </th>
                        <th>
                            <a href="/purchases/<%= purchase.id %>/edit">edit</a>
                        </th>
                        <th>
                            <form id="form_id" action="/purchases/<%= purchase.id %>" method="post" onsubmit="return confirm('Are you sure you want to destroy  ')">
                                <input type="hidden" name="_method" value="DELETE">
                                <input type="submit" name="some_name" value="Destroy">
                            </form>
                        </th> -->
                    </tr>
                <% end %>
            </tbody>
        </table>
    </article>
</main>
