Hi <%= customer.name %>,

A password reset was requested for your account. Please follow the link below.

<%= File.join(application_url, 'password_resets', customer.password_reset_token, 'edit') + "?email=#{customer.email}" %>
